version: "3"
services:
  api:
    depends_on:
      - postgres
    ports:
      - "8000:8000"
    env_file: ./.env
  postgres:
    image: postgres
    env_file: ./.env
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - postgres-db:/var/lib/postgresql/data

volumes:
  postgres-db: